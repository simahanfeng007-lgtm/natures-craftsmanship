This directory is the packaging target for the embedded Python runtime.

Final installers must replace this placeholder with:
- python/
- site-packages/
- runtime_manifest.json

The product must not depend on a user's system Python or user site-packages.
