# 任务评测与Benchmark

面向工具、Skill、执行链、回归测试、能力对比、质量报告场景的技能。目标：用评测集证明能力是否提升，避免凭感觉判断。

## 什么时候用
- 用户问这轮是否变强、是否回归、是否有新bug、工具schema是否覆盖时使用
- 需要对比两个包、两次评测、两版结果时使用

## 反触发条件（可选）
- 具体代码 bug 修复转交代码诊断
- 项目上线前整包质检可结合项目收口质检

## 标准流程
1. 明确用户目标、输入材料、风险等级和最终交付物。
2. 判断是否已有足够证据；证据不足时先读取、解析、检索或生成计划。
3. 优先调用只读工具形成证据，再调用写入/执行类工具。
4. 对结果做一致性检查：字段、来源、时间、路径、数量和可复现性。
5. 输出中文结论，区分已验证、推断、风险和未验证项。
6. 如需写入、提交、批量执行或影响外部系统，必须进入确认或只生成计划。
7. 交付时给出结果、证据、产物路径、下一步建议和回滚/复核方式。

## 工具
- `eval_case_build`
- `eval_run_plan`
- `eval_compare`
- `eval_regression_check`
- `eval_tool_schema_check`
- `eval_skill_quality_check`
- `eval_report`
