# L6 第七阶段：自学习、自愈、自迭代与多插件协作增强

本目录只提供候选、需求、审查请求、Handoff、公开摘要和质量门对象。

硬边界：不直接学习、不写 Skill、不写知识库、不生产工具、不改代码、不写文件、不跑测试、不迁移、不回滚、不热切换、不调度插件、不裸调模型/工具、不绕过 L3/L4/L5。

核心不变量：`ExecutionFirstWithinHardBoundaries` 在第七阶段转写为 `AdaptiveExecutionFirstPolicy`，即失败优先恢复，低风险自适应候选默认继续推进，但不突破硬边界。
